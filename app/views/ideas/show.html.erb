<p id="notice"><%= notice %></p>

<div id="section">
<div id="nav">
   <h3>Menu</h3> 
     <ul class="nav nav-pills nav-stacked" role="tablist">
      <li role="presentation" class="active"> <%= link_to "My ideas", user_path(current_user.id) if current_user %></li>
         <li role="presentation"><%= link_to 'Main page', root_path %></li>
         <li role="presentation"><%= link_to 'Ideas', ideas_path %></li>
       </ul>
    <hr>      
</div>
</div>


<div class="hero-unit">
	<table>
	<tr>
		<td><%= image_tag avatar_url(@idea.user), class: 'circular' %></td>
        <td><h1><%= "#{@idea.name}" %></h1></td>
    </tr>
</table>
        <p><%= @idea.description %></p>
        
  <% if current_user %>
   <button type="button" class="btn btn-warning btn-sm"><%= link_to "Jon the project", @idea %></button>
  <hr>
  <% end %>


</div>




<% if @idea.user %>
<%= link_to 'Edit', edit_idea_path(@idea) %> |
<%= link_to 'Back', ideas_path %> |
<%= link_to 'Delete', @idea, method: :delete, data: { confirm: 'Are you sure?' }, redirect_to: root_path %>
<% end %>
