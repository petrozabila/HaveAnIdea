<div id="header">	
	<div class="page-header">
  <h1 style="color: #00ff00;"><%= link_to 'Have An Idea ', root_path %><small>  welcoming innovatoins</small></h1>  
  </div> 
</div>

<nav class="navbar navbar-static">
   <div class="container">
    <div class="navbar-header">
     
      <a class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="glyphicon glyphicon-chevron-down"></span>
      </a>
    </div>
      <div class="navbar-collapse collapse">
        <ul class="nav navbar-nav">  
          <li><%= link_to "About", about_us_path %></li>
          <li><%= link_to "Contact", contact_path %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">All ideas</a>
          </li>
        </ul>
        <ul class="nav navbar-right navbar-nav">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="glyphicon glyphicon-search"></i></a>
            <ul class="dropdown-menu" style="padding:12px;">
                <form class="form-inline">
                  <%= form_tag ideas_path, :method => 'get' do %>
                    <p>
                      <%= text_field_tag :search, params[:search] %>
                      <%= submit_tag "Search", :name => nil %>
                    </p>
                  <% end %>
                </form>
             </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="glyphicon glyphicon-user"></i> <i class="glyphicon glyphicon-chevron-down"></i></a>
            <%= current_user.name if current_user %>
            <ul class="dropdown-menu">
              
              <% if current_user %>
  	              <li><%= link_to "My ideas", user_path(current_user.id) if current_user %></li>
  	              <li><%= link_to 'Logout', sessions_logout_path %></li>
                <% else %>
                	<li><%= link_to 'Registration', registration_path %></li>
                  <li><%= link_to 'Login', sessions_login_path %></li>
              <% end %>

              <li class="divider"></li>
              <li><a href="#">About</a></li>
             </ul>
          </li>
        </ul>
      </div>
    </div>
</nav><!-- /.navbar -->
